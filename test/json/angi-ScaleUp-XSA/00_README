angi-ScaleUp+XSA means a Scale-Up SAP HANA with one additional XSA "Standalone" node.
In HANA Topology this is a scale-out, but the second node is neither a index-worker nor a standby node.
The XSA standalone should have a slave nameserver-role and NO indexserver role.
