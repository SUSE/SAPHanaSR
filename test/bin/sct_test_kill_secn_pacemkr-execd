#!/bin/bash
#
# sct_test_kill_secn_pacemkr-execd - kill pacemaker-execd on secondary node
#
# (c) 2025 SUSE LLC
# Author: F.Herschel, L.Pinne.
# GNU General Public License v2. No warranty.
# http://www.gnu.org/licenses/gpl.html
#
# lib-mig-status: CHECK
# used-in:
#   NOT FOUND!
#
#
set -u
rc=1
src=${BASH_SOURCE[0]}
full_path=$(readlink -f "$src")
dir_path=$(dirname "$full_path")
source /usr/lib/SAPHanaSR-tester/sct_test_lib
source .test_properties
currSecondary=$(sctl_get_curr_secn "$node01")

echo "==== Kill pacemaker-execd ===="
# TODO signal 9 ?
sctl_kill_process "$currSecondary" "root" "pacemaker-execd" "11" "flags::"; rc="$?"
#
exit "$rc"
