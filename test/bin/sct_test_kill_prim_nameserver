#!/bin/bash
#
# sct_test_kill_prim_nameserver - kill primary master nameserver
src=${BASH_SOURCE[0]}
full_path=$(readlink -f "$src")
dir_path=$(dirname "$full_path")
source /usr/bin/sct_test_lib
source .test_properties
currPrimary=$(sctl_get_curr_prim "$node01")

echo "==== Kill nameserver ===="

sctl_kill_process "$currPrimary" "${sidadm}" "hdbnameserver" "-11"
# sleep 60
