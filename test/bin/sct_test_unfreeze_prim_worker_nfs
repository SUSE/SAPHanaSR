#!/bin/bash
#
# sct_test_unfreeze_prim_worker_nfs - unfreeze nfs on primary worker
#
# (c) 2025-2026 SUSE LLC
# Author: F.Herschel, L.Pinne.
# GNU General Public License v2. No warranty.
# http://www.gnu.org/licenses/gpl.html
#
set -u
src=${BASH_SOURCE[0]}
full_path=$(readlink -f "$src")
dir_path=$(dirname "$full_path")
source /usr/lib/SAPHanaSR-tester/sct_test_lib
source .test_properties
currPrimWorker=$(sctl_get_curr_prim_worker "$node01")

echo "==== Un-Freeze SAP HANA NFS on $currPrimWorker ===="

sctl_run_iptables "$currPrimWorker" '-D OUTPUT -p tcp -m multiport --ports 2049 -j DROP'
sleep 60
#
